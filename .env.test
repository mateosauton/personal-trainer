# Dedicated backend for browser-flow checks. Expo loads this file when
# NODE_ENV=test, leaving each developer's .env credentials untouched.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_KEY=mock
